const termWeeks = require('./termWeeks');
const password = require('./password');
const certificates = require('./certificates');
const logger = require('./logger');
const email = require('./email');

module.exports = {
  termWeeks,
  password,
  certificates,
  logger,
  email,
};
